{
  "installCommand": "if [ -d \"backend\" ]; then (cd backend && npm install) && (cd frontend && npm install); else npm install; fi",
  "buildCommand": "if [ -d \"frontend\" ]; then (cd frontend && npm run build) && cp -r frontend/dist . 2>/dev/null || true; else npm run build; fi",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "includeFiles": "backend/**",
      "maxDuration": 30
    }
  }
}
